# Admin UI Configuration

# API Base URL for backend communication
# - Leave empty (VITE_API_BASE_URL=) for production with reverse proxy (Caddy/nginx)
# - Set to http://localhost:8080 for local development without proxy
# - Set to custom URL for development against remote backend
VITE_API_BASE_URL=
